{
  "name": "ai4kids-store",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace web\"",
    "dev:client": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:web": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace web\"",
    "build": "npm run build --workspace web",
    "build:client": "npm run build --workspace client",
    "build:web": "npm run build --workspace web",
    "seed": "npm run seed --workspace server",
    "start": "npm run start --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
